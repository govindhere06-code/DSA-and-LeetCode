# Rotate Array

A solution to **LeetCode 189 - Rotate Array** using the **Array Reversal Algorithm**.

## Problem

Given an integer array `nums`, rotate the array to the right by `k` steps, where `k` is non-negative.

### Example

**Input:**
```text
nums = [1,2,3,4,5,6,7]
k = 3